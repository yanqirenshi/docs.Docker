import React from 'react';

function PageKubernetes () {
    let style = {
        root: {
            width: '100vw',
            height: '100vh',
        },
    };

    return (
        <div style={style.root}>
        </div>
    );
}

export default PageKubernetes;
